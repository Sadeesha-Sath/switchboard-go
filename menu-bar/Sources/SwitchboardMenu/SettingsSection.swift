import SwiftUI

// Placeholder for Task 8 — replaced with full settings form (connection, interval, launch-at-login).
struct SettingsSection: View {
    var body: some View {
        Text("Settings — coming in Task 8")
            .font(.caption)
            .foregroundColor(.secondary)
    }
}
